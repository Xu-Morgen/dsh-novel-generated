# I1a locks runtime configuration to environment variables.
# Relative paths are resolved from the current working directory.
NOVEL_PROJECT_ROOT=projects/demo

# I1b real OpenAI-compatible streaming smoke.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=
OPENAI_API_KEY=
# I1B_PROMPT=写一个雨夜抵达旧城的中文小说开篇，约 200 字。
