# I223 DoD：角色上下文预算提高到 8000

验收完成：verify:stage-67（含 verify:i223）全绿，265 文件 / 1288 测试、历史样本及累计 Electron 通过；专项预算与候选回归 38 项通过。复用 I222 双写作入口脚本验证独立打包程序，证据：`artifacts/desktop/ui/i223-packaged/validation.json`。新版：`artifacts/desktop/i223-release/win-unpacked/Novel Creation Tool.exe`。

交接块：刚完成 I223；下一可用 I224，未自动立项；公开合同不变；先前第一张写作卡的补写/重写选择仍待明确，预算调整没有改变该行为。

- 用户授权：角色预算提高到 8000，总额度同步增加。
- 目标 / owner：ContextAssembler 的角色层 4000 → 8000、基础上下文 16000 → 20000；完整故事上下文 owner 的总量 24000 → 28000。沿用 UTF-16 字符单位，其他层、裁剪方式、超限拒绝、IPC 不变。
- 验收：既有预算固定值、截断、总量超限与下游上下文测试；verify:i223 / verify:stage-67 全量和历史样本；桌面回归、独立打包可用。
- 明确不做：不改输出 maxTokens，不修改场景卡选择或绑定规则；第一张写作卡的补写/重写选择仍待用户明确。
- 预算政策回归：I214 冻结样本内容与身份金标保持不变；用户明确提高阈值后，其 7000 字符背景应完整保留，旧“必须截断”断言更新为完整内容保留与不截断。超过新预算的既有负向边界仍保留，不降低样本准确率标准。
