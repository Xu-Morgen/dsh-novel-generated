# 角色身份与纠错修改流程

实施完成：I219、I220、I221 已分别验收交付。I219 提交 e6f4587；I220 提交 2279d4d；I221 为本次合并功能提交。最终 `verify:stage-65` 全绿（264 文件 / 1286 测试），包含旧样本与累计桌面回归；打包版本合并 smoke 通过。

使用入口：故事资料 → 角色 → 冻结、删除与恢复角色。可直接预览冻结/恢复/删除，也可在“合并重复角色”选择原角色、保留角色、逐项资料来源和状态/知情来源，预览后确认。已接受而未完成的合并可在重开管理后继续；未接受的旧预览可取消后重建。关系冲突需先处理，不会静默删除关系。

交接：刚完成 I219–I221；下一可用迭代 I222，未自动立项。新增六个 strict additive 管理/合并方法，旧方法不变；生命周期与合并完成标记为独立运营记录。Backlog：完整合并撤销与自动语义同人识别不在本次范围；现有合并使用作者选择、引用预览、知识来源选择及可恢复执行。真实作品数据未自动合并。

授权：2026-09-10 用户要求根据《翠玉录重复角色检查》生成流程并实施。按依赖拆为连续切片，逐项 DoD、实现、验证与独立提交，不自动修改真实作品。

1. I219：阻止跨阶段重复主角；修正错误的“没有主角”提示；基础资料候选可选择复用，失败保留基础资料，只重试大纲/揭示；合并前再次校验，所有写入仍经 I11。
2. I220：角色引用影响预览、冻结/恢复及受控删除；保留历史记录和可恢复资料，明确被冻结角色被当前任务引用时的处理，strict additive IPC 与真实桌面验证。
3. I221：重复角色合并预览、资料冲突选择、当前引用迁移及历史身份留存；不机械合并知情，不替换历史正文字符串。按当前领域 owner 的可恢复事务实施。

生成方面先维护一致身份，不额外增加 LLM 调用。主角复用是作者在候选阶段的明确选择，最终计划确认展示实际角色与身份，不改变此前来源审阅记录。
